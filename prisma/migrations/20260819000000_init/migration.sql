CREATE TYPE "BankStatus" AS ENUM ('ACTIVE', 'INACTIVE');
CREATE TYPE "ConfigurationStatus" AS ENUM ('ACTIVE', 'INACTIVE');
CREATE TYPE "HistoryAction" AS ENUM ('CREATE', 'UPDATE', 'DELETE');
CREATE TABLE "banks" ("id" UUID NOT NULL, "bankCode" VARCHAR(50) NOT NULL, "bankName" VARCHAR(255) NOT NULL, "status" "BankStatus" NOT NULL DEFAULT 'ACTIVE', "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP, "updatedAt" TIMESTAMP(3) NOT NULL, CONSTRAINT "banks_pkey" PRIMARY KEY ("id"));
CREATE TABLE "service_configurations" ("id" UUID NOT NULL, "bankId" UUID NOT NULL, "serviceName" VARCHAR(100) NOT NULL, "config" JSONB NOT NULL, "version" INTEGER NOT NULL DEFAULT 1, "status" "ConfigurationStatus" NOT NULL DEFAULT 'ACTIVE', "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP, "updatedAt" TIMESTAMP(3) NOT NULL, CONSTRAINT "service_configurations_pkey" PRIMARY KEY ("id"));
CREATE TABLE "configuration_history" ("id" UUID NOT NULL, "configurationId" UUID NOT NULL, "version" INTEGER NOT NULL, "action" "HistoryAction" NOT NULL, "previousConfig" JSONB, "newConfig" JSONB, "changedBy" VARCHAR(255), "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP, CONSTRAINT "configuration_history_pkey" PRIMARY KEY ("id"));
CREATE UNIQUE INDEX "banks_bankCode_key" ON "banks"("bankCode"); CREATE INDEX "banks_bankCode_idx" ON "banks"("bankCode"); CREATE UNIQUE INDEX "service_configurations_bankId_serviceName_key" ON "service_configurations"("bankId","serviceName"); CREATE INDEX "service_configurations_bankId_idx" ON "service_configurations"("bankId"); CREATE INDEX "service_configurations_serviceName_idx" ON "service_configurations"("serviceName"); CREATE INDEX "service_configurations_bankId_serviceName_idx" ON "service_configurations"("bankId","serviceName"); CREATE INDEX "configuration_history_configurationId_idx" ON "configuration_history"("configurationId"); ALTER TABLE "service_configurations" ADD CONSTRAINT "service_configurations_bankId_fkey" FOREIGN KEY ("bankId") REFERENCES "banks"("id") ON DELETE CASCADE ON UPDATE CASCADE; ALTER TABLE "configuration_history" ADD CONSTRAINT "configuration_history_configurationId_fkey" FOREIGN KEY ("configurationId") REFERENCES "service_configurations"("id") ON DELETE CASCADE ON UPDATE CASCADE;
